<!-- skyramp-pom-catalog: auto-generated by skyramp_reuse_code -->

## FunctionsPage
<!-- depth: full -->
- **Import:** `../../pages/functionsPages/functionsPage.js` (CommonJS; incompatible with TypeScript Skyramp runner — no tsconfig path aliases defined)
- **Iframe:** top-level page
- **Properties:**
  - `addFunctionButton` — selector: `[data-test="function-list-add-function-btn"]`
  - `vrlRadio` — selector: `[data-test="function-transform-type-vrl-option"]` (updated from -vrl-radio by maintenance)
  - `jsRadio` — selector: `[data-test="function-transform-type-js-option"]` (updated from -js-radio by maintenance)
  - `functionNameTrigger` — selector: `[data-test="add-function-name-input-trigger"]`
  - `saveButton` — selector: `[data-test="add-function-save-btn"]`
  - `cancelButton` — selector: `[data-test="add-function-cancel-btn"]`
  - `searchInputField` — selector: `[data-test="functions-list-search-input-field"]`
- **Methods:**
  - `navigate(org)` — navigates to pipeline/functions page; ⏱ timing-aware: waitForTimeout(2000)
  - `clickAddFunctionButton(org)` — clicks add button with auto-recovery; ⏱ timing-aware: waitForTimeout(500, 1000)
  - `selectJavaScriptType()` — clicks jsRadio; ⏱ timing-aware: waitForTimeout(1500)
  - `selectVRLType()` — clicks vrlRadio; ⏱ timing-aware: waitForTimeout(1500)
  - `expectVrlRadioVisible()` — asserts vrlRadio visible
  - `expectJsRadioVisible()` — asserts jsRadio visible
  - `expectJsRadioSelected()` — asserts jsRadio has data-state=on
  - `expectVrlRadioSelected()` — asserts vrlRadio has data-state=on

## FunctionsFormValidationPage
<!-- depth: full -->
- **Import:** `../../pages/functionsPages/functionsFormValidationPage.js` (CommonJS; incompatible with TypeScript Skyramp runner)
- **Iframe:** top-level page
- **Properties:**
  - `addFunctionButton` — selector: `[data-test="function-list-add-function-btn"]`
  - `vrlRadio` — selector: `[data-test="function-transform-type-vrl-option"]` (updated by maintenance)
  - `jsRadio` — selector: `[data-test="function-transform-type-js-option"]` (updated by maintenance)
  - `saveButton` — selector: `[data-test="add-function-save-btn"]`
  - `cancelButton` — selector: `[data-test="add-function-cancel-btn"]`
- **Methods:**
  - `openAddFunctionForm(org)` — navigates and clicks add button; ⏱ timing-aware: waitForTimeout(networkidle)
  - `assertVrlRadioSelected()` — asserts vrl option has data-state=on

## LoginPage
<!-- depth: full -->
- **Import:** `../../pages/generalPages/loginPage.js` (CommonJS; incompatible with TypeScript Skyramp runner)
- **Iframe:** top-level page
- **Properties:**
  - `userIdInput` — selector: `[data-test="login-user-id-field"]`
  - `passwordInput` — selector: `[data-test="login-password-field"]`
  - `loginButton` — selector: `[data-test="login-sign-in"]`
- **Methods:**
  - `fillLoginForm(email, password)` — fills both login fields
  - `submitLoginForm()` — clicks loginButton
  - `gotoLoginPage()` — navigates to base URL
  - `login()` — full login sequence

## homePage
<!-- depth: signature -->
- **Import:** `../../pages/generalPages/homePage.js` (CommonJS)
- **Matched selectors:** `function-list-add-function-btn`
- **Methods:** *(signature only — not deepened)*

## sanityPage
<!-- depth: signature -->
- **Import:** `../../pages/generalPages/sanityPage.js` (CommonJS)
- **Matched selectors:** `function-list-add-function-btn`

## pipelinesEP
<!-- depth: signature -->
- **Import:** `../../pages/pipelinesPages/pipelinesEP.js` (CommonJS)
- **Matched selectors:** `function-list-add-function-btn`
