<!-- skyramp-pom-catalog: auto-generated by skyramp_reuse_code -->

## LoginPage
- **Import:** `../../pages/generalPages/loginPage.js` (ES module — `export class LoginPage`)
- **Iframe:** top-level page
- **Properties:**
  - `userIdInput` — selector: `[data-test="login-user-id-field"]`
  - `passwordInput` — selector: `[data-test="login-password-field"]`
  - `loginButton` — selector: `[data-test="login-sign-in"]`
  - `userIdError` — selector: `[data-test="login-user-id-error"]`
  - `passwordError` — selector: `[data-test="login-password-error"]`
  - `homeMenuItem` — selector: `[data-test="menu-link-\\/-item"]`
- **Methods:**
  - `openInternalLoginForm()` — navigates to login page, clicks internal user link, waits for form visibility
  - `fillLoginForm(email, password)` — fills userIdInput and passwordInput
  - `submitLoginForm()` — clicks loginButton
  - `login()` — full login using `ZO_ROOT_USER_EMAIL`/`ZO_ROOT_USER_PASSWORD` env vars; 🌐 network-aware: waitForResponse('/auth/login'); ⏱ timing-aware: waitForTimeout(2000)
  - `gotoLoginPage()` — navigates to `ZO_BASE_URL`
  - `loginAsInternalUser()` — clicks 'Login as internal user' link if present

## FunctionsPage
<!-- depth: full -->
- **Import:** `../../pages/functionsPages/functionsPage.js` (CommonJS — `module.exports = FunctionsPage`; requires `require()` in TypeScript)
- **Iframe:** top-level page
- **Properties:**
  - `addFunctionButton` — selector: `[data-test="function-list-add-function-btn"]`
  - `searchInputWrapper` — selector: `[data-test="functions-list-search-input"]`
  - `searchInputField` — selector: `[data-test="functions-list-search-input-field"]`
  - `bulkDeleteButton` — selector: `[data-test="function-list-delete-functions-btn"]`
  - `rowEditButtonSelector` — selector string: `[data-test="function-list-edit-function-btn"]`
  - `rowDeleteButtonSelector` — selector string: `[data-test="function-list-delete-function-btn"]`
  - `functionNameTrigger` — selector: `[data-test="add-function-name-input-trigger"]`
  - `functionNameInputField` — selector: `[data-test="add-function-name-input-input"]`
  - `functionEditor` — selector: `[data-test="logs-vrl-function-editor"]`
  - `saveButton` — selector: `[data-test="add-function-save-btn"]`
  - `cancelButton` — selector: `[data-test="add-function-cancel-btn"]`
  - `backButton` — selector: `[data-test="add-function-back-btn"]`
- **Methods:**
  - `navigate(org)` — navigates to `/web/pipeline/functions?org_identifier=<org>`; ⏱ timing-aware: waitForTimeout(2000)
  - `clickAddFunctionButton(org)` — clicks addFunctionButton with auto-recovery navigation; ⏱ timing-aware: waitForTimeout(500/1000)
  - `fillFunctionName(name)` — clicks functionNameTrigger then fills functionNameInputField
  - `getFunctionNameCell(name)` — locator factory for `[data-test="function-list-name-cell-<name>"]`
  - `getRowByName(name)` — locator factory: walks up from name cell to OTable row
  - **NOTE:** `function-list-duplicate-function-btn` is NOT in this POM (added by this PR — no POM coverage yet)
